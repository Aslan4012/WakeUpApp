import 'package:flutter/material.dart';

class DropDivider extends StatelessWidget {
  const DropDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Color(0xff808080),
      thickness: 1,
    );
  }
}
